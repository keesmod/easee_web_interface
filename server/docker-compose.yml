version: '3.8'
services:
  easee-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - SESSION_SECRET=${SESSION_SECRET:-change_me}
      - EASEE_API_BASE=${EASEE_API_BASE:-https://api.easee.com}
    restart: unless-stopped

